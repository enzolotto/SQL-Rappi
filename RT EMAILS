SELECT RT_ID, EMAIL
FROM BR_PG_MS_RT_REGISTRATION_PUBLIC.STOREKEEPER_REQUEST SKR
LEFT JOIN BR_PG_MS_RT_REGISTRATION_PUBLIC.APPLICANT_USER AU
ON SKR.APPLICANT_USER_ID = AU.ID
WHERE RT_ID IS NOT NULL LIMIT 100
